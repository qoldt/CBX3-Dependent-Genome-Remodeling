# macOS
.DS_Store

# R
.Rhistory
.RData
.Rproj.user/

# Generated analysis outputs — regenerable from the pipeline.
# results/rds/ is intentionally NOT ignored (the handoff .rds is tracked).
ChIP/results/counts/
ChIP/results/tables/
ChIP/results/figures/
ChIP/results/bed/

# Large raw inputs — provided locally via $MINUTE_DATA, never committed
bigwig/
*.bw
LINE.mm39.bed
SINE.mm39.bed
LTR.mm39.bed
