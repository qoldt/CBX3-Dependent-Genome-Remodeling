# macOS
.DS_Store

# R
.Rhistory
.RData
.Rproj.user/

# Generated analysis outputs — regenerable from the pipeline.
# results/rds/ is intentionally NOT ignored (the handoff .rds is tracked).
ChIP/results/counts/
ChIP/results/tables/
ChIP/results/figures/
ChIP/results/bed/
# large terminal cache (regenerable; not a downstream handoff)
ChIP/results/rds/repeat_class_signal.rds

# Large raw inputs — downloaded from Google Drive (see ChIP/README.md), never
# committed. Peaks (ChIP/data/peaks) ARE committed. Default download targets:
ChIP/data/bigwig/
ChIP/data/annotation/
# Belt-and-suspenders in case MINUTE_DATA points somewhere else in the tree:
bigwig/
*.bw
LINE.mm39.bed
SINE.mm39.bed
LTR.mm39.bed
TAD_boundaries_mm39.bed

# stray base-R plot device output
Rplots.pdf
